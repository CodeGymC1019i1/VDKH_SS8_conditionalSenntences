function whatName() {
    let String = prompt('tên chính thức của javascript là gì ? ');
    if (String === 'ECMAScript') {
        alert('bạn đã trả lời đúng')
    } else {
        alert('bạn đã trả lời sai');
    }
}